"""foss_mcp — MCP servers for the Aspose FOSS product line.

This is the root of the ``foss_mcp`` package tree. Its structure is fixed by
plan 11.3 and documented, package by package, in
``docs/REPOSITORY_LAYOUT.md``. That document is the authority for where a
given file belongs; when in doubt, it wins over local convention.
"""
